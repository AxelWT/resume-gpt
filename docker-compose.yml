services:
  app:
    image: ghcr.io/${GITHUB_REPOSITORY:-your-org/ai-interview-coach}:latest
    build: .
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
